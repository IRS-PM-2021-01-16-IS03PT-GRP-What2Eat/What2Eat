is_healthy
	use heath(food)
	when
		heatlh.food('Heathy')
		check $Meat >=1 && $Vege >= 2 %% $Fruit >=2
is_meat
	use meat(food)
	when
		heatlh.food('Carnivore')
		check $Meat >=2 

is_vegetarian
	use vege(food)
	when
		heatlh.food('Vegetarian')
		check $Vege >= 2
